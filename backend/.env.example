# Port the backend server listens on
BACKEND_PORT=3000

# API key for plugin webhook authentication
API_KEY=your-plugin-api-key

# Password for frontend/WebSocket authentication
FRONTEND_PASSWORD=your-dashboard-password

# SQLite database path
DATABASE_URL=./data/database.db

# Optional: path to frontend build for static serving
# STATIC_DIR=../frontend-svelte/build

# Optional: disable TTS announcements
# DISABLE_TTS=true
