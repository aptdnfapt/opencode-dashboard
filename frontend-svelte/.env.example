# API key for authenticating with the backend
VITE_API_KEY=your-api-key

# Password for dashboard login
VITE_FRONTEND_PASSWORD=your-frontend-password

# Optional: override WebSocket URL (auto-detected if not set)
# VITE_WS_URL=ws://localhost:3000/ws

# Optional: backend port for dev mode WS connection (default: 3000)
# VITE_BACKEND_PORT=3000
